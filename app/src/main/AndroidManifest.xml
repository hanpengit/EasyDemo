<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hunder.easydemo">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".dialog.DialogActivity" />
        <activity android:name=".view.ViewActivity" />
        <activity android:name=".view.DragViewActivity" />
        <activity android:name=".view.PieViewActivity" />
        <activity android:name=".network.NetWorkSpeedTestActivity" />
        <activity android:name=".emoji.EmojiTestActivity" />
        <activity android:name=".network.NetworkListeningActivity" />
        <activity android:name=".CommonWebActivity" />
        <activity android:name=".play.SurfaceActivity" />
        <activity android:name=".play.SurfaceActivity2" />
        <activity android:name=".view.VolumeViewActivity" />
        <activity android:name=".view.CustomView1Activity" />
        <activity android:name=".view.SmoothScrollViewActivity" />
        <activity android:name=".view.SmoothScrollViewActivity1" />
    </application>

</manifest>